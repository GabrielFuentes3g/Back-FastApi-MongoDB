# Back-FastApi-MongoDB


# -----instalacion de paquetes -----

# pip install fastapi (framework de fastapi)

# pip install uvicorn (paquete para iniciar el servidor)

# pip install pymongo (modulo de conexion para bd)



# -----Comandos-----

# uvicorn main:main --reload (Inicial el servidor)